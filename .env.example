OPENAI_API_KEY=

# Optional: override the model used for generation and query rewriting.
OPENAI_CHAT_MODEL=gpt-4o
OPENAI_EMBED_MODEL=text-embedding-3-small

# Optional: where the Chroma index is persisted so the corpus is not
# re-embedded on every run.
CHROMA_DIR=./chroma_db
